# Maps

여기에 만들 것:
- `L_Step0_TestVillage` — `BP_WorldClock` 1개 + `BP_ScheduleLocation` 3개(Home/TrainingGround/Market) + `BP_NPC_Base` 1개 배치.

만든 뒤 Project Settings > Maps & Modes > Editor Startup Map을 이 레벨로 지정해두면 다음에 열 때 편하다.
