# Data/Tables

여기 만드는 것:
- `DT_Schedule_TestNPC` — `Data/Schedule_TestNPC.csv`를 임포트해서 생성 (Row Struct는 C++ `FScheduleEntry`, 자동으로 선택됨).

`Data/Schedule_TestNPC.csv`만 고치고 DataTable을 리임포트하면 NPC 일과가 바뀌는지가 Step 0의 성립 조건이다.
