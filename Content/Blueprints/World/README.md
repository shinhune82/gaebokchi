# Blueprints/World

여기에 만들 것 (`docs/step0-npc-schedule.md` 5절 참고):
- `BP_ScheduleLocation` — Target Point 기반 장소 마커. `LocationTag` 변수로 DataTable의 `LocationTag`와 매칭.

레벨에 최소 3개 배치: `Loc.Home`, `Loc.TrainingGround`, `Loc.Market`.
